#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a;
	cout<<"Enter first number, operator, second number: ";
	getline(cin,a);
	float x;
	x=atoi(a.c_str());
	cout<<"Answer = "<<a;
	return 0;
}
